#!/bin/sh
# Public-safe stub of the GitHub Copilot CLI for the copilotline doctor demo.
# Returns a fixed version banner so `copilotline doctor` renders a deterministic
# "copilot command available" line without depending on a real Copilot install.
echo "GitHub Copilot CLI 1.0.0"
